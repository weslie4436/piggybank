# PiggyBank Collaboration Rules

## 專案一句話

孩子每天領零用金養滿撲滿、收割儲蓄收益，消費時親手敲豬並由父母掃 QR＋PIN 核准。

## 入口與資料邊界

- 唯一使用者入口是 GitHub Pages；桌機、iPad、iPhone 共用同一網址。
- Pages 只放靜態門面與可傳入口鑰匙；帳本、PIN、個人鑰匙與 SQLite 只留家裡保險庫。
- 禁止把 exe、bat、cmd 當產品入口。
- 網頁入口 → `config.js` 的 `VAULT_ORIGIN` → 家裡 Python HTTP 保險庫＋Cloudflare 隧道。

## 門面

- 門面用語與幾何見 skill `fami-shared-ui`；三端細節走 `ios-home-web`。
- 返回、確認、操作卡、問句卡、齒輪、等待、首頁頭、格子、愛心與多選工具列全部沿用共用件。
- 產品專有物件只有撲滿舞台、六格固定倉庫頁與兌換卡。
- 第一版撲滿使用佔位方塊；金幣使用使用者提供素材。
- 標籤選單第一顆文字固定「最愛」。

## 第一版經濟規則

- 基礎豬容量 150 元、敲 5 下、滿後每日每隻產 1 元。
- 每隻未收收益最多 3 元；孩子手動收割，收益留在原豬。
- 主畫面只有一隻正在養的豬；零用金直接灌入，溢額接續下一隻。
- 倉庫每頁固定 6 格；六隻完整滿 24 小時後，整頁每日額外產 2 元，最多累積 3 天（6 元）。
- 頁面固定不自動搬豬；新滿豬優先補最前面的洞。完整六隻後自動開下一頁。
- 只有滿豬能敲。找零自動灌回正在養的豬；父母核准前只保留，不永久破壞。

## 帳務硬規則

- SQLite 交易是唯一真相；前端動畫不得自行推算最終餘額。
- 同週期只能領一次、同 QR 只能完成一次、餘額不得為負。
- 帳本只追加；更正以反向紀錄完成，不刪改歷史。
- 日期與收益一律以保險庫的 Asia/Taipei 時間判定。

## Git 與部署

- 只在 `main` 工作，禁止 PR。
- Pages 檔驗證後提高 `?v=`，commit 並 push `origin main`。
- 不提交 `data/`、SQLite、PIN、鑰匙、隧道暫存狀態或測試產物。
