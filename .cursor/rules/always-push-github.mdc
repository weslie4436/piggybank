---
description: PiggyBank Pages 改完必推 main
alwaysApply: true
---

# 修完就推 Pages

會出現在 GitHub Pages 的 html／js／css／圖示／`config.js`／`?v=`：驗證後 `commit` + `push origin main`，再把 `web/` 發到 `gh-pages`（Pages 吃的是這個分支，不是 main 根目錄）。只推 main = 線上還是舊門面。不要問要不要推。禁止 PR。

```
git subtree split --prefix web
git push origin <sha>:gh-pages
```

- Pages：https://weslie4436.github.io/piggybank/
- 保險庫：`python -m piggybank vault`（埠 8771）
- 隧道 origin 寫在 `web/config.js` 的 `window.VAULT_ORIGIN`
- 不提交 `data/`、SQLite、PIN、個人鑰匙
